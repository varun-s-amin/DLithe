#include "student.hpp"

static void create(Student *student) {
    // file db - open
    // write student into file db
    //file db - close
}

//definitions of student.hpp functions
void createStudent(Student *student) {
    create(student);
}